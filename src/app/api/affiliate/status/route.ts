import { NextResponse } from 'next/server'; export async function GET() { return NextResponse.json({ message: 'Affiliate system offline' }); } export async function POST() { return NextResponse.json({ message: 'Affiliate system offline' }); }